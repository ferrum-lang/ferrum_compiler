use ./utils::hello

pub fn main()
    mut p = Person {
        name: "Snowy",
        age: 26,
    }
    hello(&"{p.age} year-old named {p.name}")

    p.age = 27
    hello(&"{p.age} year-old named {p.name}")
;

struct Person {
    name: String,
    age: Int,
}
