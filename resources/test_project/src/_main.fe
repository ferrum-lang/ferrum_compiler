use ./utils::hello

pub fn main()
    mut p = Person {
        name: "Snowy",
    }
    hello(&p.name)

    const age = 26

    hello(&"{age} year-old named {p.name}")
;

struct Person {
    name: String,
}
